普通用户：asdf 密码：111111
管理员用户：admin 密码：admin
启动应用：通过执行AppRun类的main方法来启动应用程序。
分层架构：系统采用了分层架构，主要分为表现层（common）、数据访问层（pojo）、业务逻辑层（start）和工具类层（utils）。

          模块化设计：系统功能模块化，如用户登录（Login）、用户注册（Register）、管理员登录（AdminLogin）等，每个模块负责特定的功能。

          数据库交互：通过Connect类与数据库进行交互，执行查询和更新操作。
